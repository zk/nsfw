#!/bin/bash
# Remove files generated by build scripts

# Script Dir
pushd `dirname $0` > /dev/null
SP=`pwd`
popd > /dev/null

pushd $SP/.. > /dev/null

echo " * Removing generated assets"

rm -rf ./target
rm -rf ./resources/public/cljs

popd > /dev/null