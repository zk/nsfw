#!/bin/bash

set -e

# Script Dir
pushd `dirname $0` > /dev/null
SP=`pwd`
popd > /dev/null


pushd $SP/.. > /dev/null

bin/clean

lein cljsbuild once prod
lein with-profile production compile :all

popd > /dev/null
